# Expense Tracker

This repository contains a Gmail expense tracker that parses receipts and transaction emails, stores them in SQLite, and syncs expenses to Google Sheets.

## Important
- Do not commit `expense-tracker/credentials.json` or `expense-tracker/token.json`.
- These files are personal and contain sensitive OAuth credentials.
- Use `expense-tracker/credentials.example.json` as a template when adding y- Use `expense-tracker/credeal se- Use `expense-trackerracker- Use `expense-tracker/credeo `e- Use `era- er- Use `expense-tracker/credentials.example.json` ass from- Use `expense-3.- Use `expense-tracker/credentials.example.json` as a templaui- Use `expense-tracker/credentiaker && .venv/bin/python3 agent.py --setup`.

## Uploading to GitHu## Uploading to GitHu## Uploading to GitHu## Uploadingntials, token data, and the Python virtual environment.
